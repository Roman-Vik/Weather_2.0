import React, {FC} from "react";
import s from "./MonthStatistic.module.sass"

interface PropsHome {
    
}

export const MonthStatistic: FC<PropsHome> = () => {
  return <div className={s.home}>
    MonthStatistic
  </div>
}